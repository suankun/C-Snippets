#include <stdio.h>

int main()
{
    int x = 5;
    int y = 10;
    int rate = 1;

    printf("x = %d\n", x);
    printf("%d + %d = %d\n", x, y, x + y);
    printf("Rate is %d%%\n", rate * 100);
    
    return 0;
}